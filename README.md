# case-intake-handoff-automation-system
An n8n and Google Sheets workflow that validates incoming cases, routes them by service type, assigns owners, sets due dates, and creates structured internal handoff records.
